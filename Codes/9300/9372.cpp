#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int t;
	cin >> t;
	int n, m;
	while(t--) {
		cin >> n >> m;
		for(int i = 0; i < m; ++i) {
			int x, y;
			cin >> x >> y;
		}
		cout << n - 1 << '\n';
	}
	return 0;
}
