#include <bits/stdc++.h>
using namespace std;

void handle_case() {
    int count;
    cin >> count;

    int size_limit, weight_limit;
    cin >> size_limit >> weight_limit;

    for (int i = 0; i < count; ++i) {
        int size, weight;
        cin >> size >> weight;
    }
}

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);

    int t;
    cin >> t;
    for (int i = 1; i <= t; ++i) {
        handle_case();
        cout << "Material Management " << i << '\n';
        cout << "Classification ---- End!\n";
    }
    return 0;
}
