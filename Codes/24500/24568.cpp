#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    int r, s;
    cin >> r >> s;

    int total = 8 * r + 3 * s;
    cout << total - 28;
    return 0;
}
