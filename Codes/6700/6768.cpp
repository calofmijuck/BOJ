#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    int j;
    cin >> j;
    cout << (j - 1) * (j - 2) / 2 * (j - 3) / 3;
    return 0;
}
