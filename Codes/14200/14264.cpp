#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    double l;
    cin >> l;
    cout << fixed << setprecision(10) << l * l * sqrt(3) / 4;
    return 0;
}
